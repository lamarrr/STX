use std::thread;

pub fn main(){

thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
thread::spawn(|| panic!("at the disco"));
}
